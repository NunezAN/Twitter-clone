import React from 'react';
import "./Post.css"

const Post = (displayName, userName, verified, text, image, avatar) => {
    return (
        <div className='Post'>
            
        </div>
    );
}

export default Post;
